# Pinned native dependency versions - single source of truth, shared by
# scripts/fetch-native-deps.sh and the CI workflows. Environment variables
# still override these when set.
#
# The piper1-gpl / espeak-ng / sonic revisions are pinned as git submodule
# SHAs instead (see .gitmodules); bump them with e.g.
#   git -C third-party/piper1-gpl checkout <new-sha> && git add third-party/piper1-gpl
ORT_VERSION=1.22.0
NDK_VERSION=27.0.12077973
